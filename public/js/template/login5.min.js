const swal = Swal.mixin({ customClass: { confirmButton: "btn btn-primary" }, buttonsStyling: !1 }); document.addEventListener("DOMContentLoaded", (function () { const e = document.querySelector("form"); e && e.addEventListener("submit", (function (e) { e.preventDefault(); const t = document.getElementById("phone").value, a = document.getElementById("otp").value; t.length < 10 ? Swal.fire({ icon: "error", title: "Oops...", text: "Nomor Whatsapp minimal harus 10 karakter!" }) : fetch("/api/verify-otp", { method: "POST", headers: { "Content-Type": "application/x-www-form-urlencoded", Accept: "application/json" }, body: new URLSearchParams({ phone: t, otp: a }) }).then((async e => { if (!e.ok) { throw await e.json().catch((() => ({ message: "Terjadi kesalahan pada server" }))) } return e.json() })).then((e => { Swal.fire({ icon: "success", title: e.message, text: "Anda telah berhasil masuk." }).then((() => { let e = new URLSearchParams(window.location.search).get("redirect_url"); window.location.href = e || "/daily" })) })).catch((e => { Swal.fire({ icon: "error", title: "Oops...", text: e.message || "Terjadi kesalahan pada server" }) })) })); const t = document.getElementById("send-otp"); t && t.addEventListener("click", (function () { const e = document.getElementById("phone").value; if (e.length < 10) return void Swal.fire({ icon: "error", title: "Oops...", text: "Nomor Whatsapp minimal harus 10 karakter!" }); let t = navigator.userAgent; if (/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(t)) { fetch("/api/mobile-otp", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ phone: e }) }); const t = "undefined" != typeof admin ? admin : ""; window.open("https://wa.me/+62" + t + "?text=OTP", "_blank") } else this.disabled = !0, setTimeout((function () { document.querySelectorAll("button").forEach((e => e.disabled = !1)) }), 3e4), fetch("/api/mobile-otp", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ phone: e }) }), Swal.fire({ title: "Sedang mengirim OTP", allowOutsideClick: !1, didOpen: () => { swal.showLoading() } }), fetch("/api/send-otp", { method: "POST", headers: { "Content-Type": "application/x-www-form-urlencoded", Accept: "application/json" }, body: new URLSearchParams({ phone: e }) }).then((async e => { if (!e.ok) { throw await e.json().catch((() => ({ message: "Terjadi kesalahan pada server" }))) } return e.json() })).then((e => { Swal.fire({ icon: "success", title: "Success", text: e.message }) })).catch((e => { Swal.fire({ icon: "error", title: "Oops...", text: e.message || "Terjadi kesalahan pada server" }) })) })) })), sessionStorage.clear();