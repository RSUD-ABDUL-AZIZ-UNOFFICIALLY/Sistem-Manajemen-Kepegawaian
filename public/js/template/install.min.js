let deferredPrompt; const installModal = new bootstrap.Modal(document.getElementById("installModal")); function installApp() { deferredPrompt.prompt(), deferredPrompt.userChoice.then((choiceResult => { "accepted" === choiceResult.outcome ? console.log("Aplikasi diinstal") : console.log("Pengguna menolak untuk menginstal aplikasi"), deferredPrompt = null, installModal.hide() })) } window.addEventListener("beforeinstallprompt", (event => { event.preventDefault(), deferredPrompt = event, installModal.show(); document.getElementById("installButton").addEventListener("click", (() => { installApp() })) })), console.log("Halo");
