const swal = Swal.mixin({ customClass: { confirmButton: "btn btn-primary" }, buttonsStyling: !1 }); $("form").on("submit", (function (e) { e.preventDefault(); const t = $("#phone").val(), o = $("#otp").val(); t.length < 10 ? swal.fire({ icon: "error", title: "Oops...", text: "Nomor Whatsapp minimal harus 10 karakter!" }) : $.ajax({ url: "/api/verify-otp", method: "POST", data: { phone: t, otp: o }, success: function (e) { swal.fire({ icon: "success", title: e.message, text: "Anda telah berhasil masuk." }).then((() => { let e = new URLSearchParams(window.location.search).get("redirect_url"); console.log(e), window.location.href = e || "/daily" })) }, error: function (e, t, o) { swal.fire({ icon: "error", title: "Oops...", text: e.responseJSON.message }) } }) })), $("#send-otp").on("click", (function () { const e = $("#phone").val(); if (e.length < 10) return void swal.fire({ icon: "error", title: "Oops...", text: "Nomor Whatsapp minimal harus 10 karakter!" }); let t = navigator.userAgent; if (/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(t)) return fetch("/api/mobile-otp", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ phone: e }) }), void window.open("https://wa.me/+62" + admin + "?text=OTP", "_blank"); $(this).prop("disabled", !0), setTimeout((function () { $("button").prop("disabled", !1) }), 3e4), swal.fire({ title: "Sedang mengirim OTP", allowOutsideClick: !1, onBeforeOpen: () => { swal.showLoading() } }), $.ajax({ url: "/api/send-otp", method: "POST", data: { phone: e }, success: function (e) { swal.fire({ icon: "success", title: "Success", text: e.message }) }, error: function (e, t, o) { swal.fire({ icon: "error", title: "Oops...", text: e.responseJSON.message }) } }) })), sessionStorage.clear();